# 实施计划 —— SPEC B：resume / 重放幂等裂缝修复

> 依据：[`docs/specs/2026-08-02-audit-b.md`](../specs/2026-08-02-audit-b.md)（r4 conditional-pass）
> 实施顺序（强制）：**B2 → B1 → B3 → MINOR**（B1 skipped-window fallback 依赖 B2 的 outputs_acc 形状）

## B2 — reducer 持久层（先做）

1. `orca/events/replay.py::node_skipped`：补 `context = {**state.context, node: None}`；`return state.model_copy(update={"node_status": node_status, "context": context})`。
2. `orca/run/orchestrator.py:671`：live skip 改 `outputs_acc[current] = {"output": None}`（删 `"skipped": True`）。
3. `orca/run/router.py:77,93-105,107`：注释中旧形状 `{"output":None,"skipped":True}` 改为新形状 `{"output":None}` + 说明 skipped 派生自 `node_status`。
4. 测试（`tests/events/test_replay.py`）：
   - 既有 `test_node_failed_and_skipped_branches`（:212）补 `assert s.context["b"] is None`。
   - 新增 `test_reducer_node_skipped_writes_context_raw_none`：`context[N] is None`（raw，非 dict）。
   - 新增 `test_reducer_node_skipped_idempotent`。
   - 新增 `test_old_tape_skipped_replays_with_context`。

## B1 — resume 起点（B2 落地后做）

1. `orca/run/resume.py`：新建 `replay_for_resume(tape) -> (state, inputs, last_progressed)` —— 单 `tape.replay()` 迭代，调 events 层公共 `apply_event`，顺手抽 inputs（mirror `_replay_state_and_inputs` 语义含 WARNING）+ last_progressed（最后 `node_completed` OR `node_skipped` 的 node 名）。
2. `orca/run/orchestrator.py::from_tape`：
   - 删除独立 `replay_state(tape)` + `_find_last_done_node_name` 调用，改用 `replay_for_resume(tape)` 三元组。
   - resume 判据：`cur_status = state.node_status.get(resume_node)`；当 `resume_node is None/$end` 或 `cur_status in ("done","skipped")` 时走 progressed fallback（消费 `last_progressed`）；fallback 内若无任何 progressed → `wf.entry`；progressed 非空但 `last_progressed is None` → `raise MidFileCorruptError(...)`（defense-in-depth）。
3. `_bare_instance` 签名：增 `inputs: dict[str, Any]` 参数；删除内部 `_inputs_from_tape` 调用。
4. **E5 删死代码**：`_inputs_from_tape` wrapper（:534-560）成零调用方，删除；`tests/events/test_replay.py:877-941` parity 测试 + `:944-979` AST 守门测试一并删除（wrapper 已不存在）。
5. `_find_last_done_node_name`（:562-575）删除（B1 直消费 `last_progressed`）。
6. `run_from_state`：`workflow_resumed.data` 增 `current_node_at_crash`（值 = `state.current_node`，additive）；`_bare_instance` 多存 `_resume_current_node_at_crash`。
7. **E4**：`tests/run/test_resume.py:362` 收紧 `assert orch._resume_start_node == "b"`；清理 :330/:333-336/:356-361 stale 注释。
8. **E2**：`replay_for_resume` 必须保留「ws 存在但 inputs 坏」WARNING（mirror `orca/events/replay.py:88-93`）。
9. 新建 `tests/run/test_resume_crash_window.py`：
   - `test_resume_after_node_completed_before_route_taken`
   - `test_resume_after_skip_then_crash`（done A→skip B→route B→C→崩 C running）
   - `test_resume_after_node_skipped_before_route_taken`
   - `test_resume_idempotent_reapplication`

## B3 — 删 RunState.usage 字段（E9 单 commit，保留 UsageSummary 类）

1. `orca/schema/state.py:64`：删 `usage: UsageSummary | None = None`。
2. `orca/events/replay.py:186-193`：docstring 改为「显式 no-op：session 级 usage 不进顶层 RunState；用 `projections.node_usage` 派生」。
3. `orca/iface/web/run_manager.py:1951-1957`：`_extract_cost` 改读 `projections.node_usage(tape.replay())` 求和（**E1**：修 `getattr(usage,"cost",...)` typo 为 `cost_usd`，但实际改为 projections 后该 typo 消失）。
4. **E9 同 commit**：改 `tests/schema/test_state.py:21,32,36` 三处。
5. `tests/events/test_replay.py`：新增 `test_runstate_has_no_usage_field` + `test_agent_usage_reducer_is_noop`。
6. **E1 §8.3**：TUI cost e2e 锚点断言「非零 cost」（构造 agent_usage 含 `cost_usd>0` 的 demo，断言 TUI 显示>0），不与坏基线对齐。

## MINOR — spy 测试锁

1. `tests/run/test_resume.py::test_from_tape_two_fold`（**E3**）：spy 两处入口：
   - `mock.patch("orca.run.resume._find_first_corrupt_line", wraps=...)`：1 次（raw open 不走 tape.replay）。
   - `mock.patch("orca.events.tape.Tape.replay", wraps=...)`：1 次（`replay_for_resume` 单 pass）。
   - 总和 = 2（**E3 不 spy `builtins.open`**）。
2. `test_bare_instance_no_inputs_from_tape`：删除（wrapper 已删，测试目标不存在）。改为 spy/AST 断言「`orca/` 内零 `_inputs_from_tape` attribute call」。

## 自我 Review 核对点

- 依赖铁律：events 层仍是 reducer fold 唯一入口；run 层 `replay_for_resume` 调 events 层公共 `apply_event`，不反向 import。
- fail loud：未知事件仍 WARNING； MidFileCorruptError defense-in-depth 保留。
- 幂等：`node_skipped` 多次应用 = 一次（覆盖语义）。
- 向后兼容：老 tape（含 node_skipped）重放新 reducer 自动补 context[N]=None。
- 4→2 spy 实测：corrupt(1) + tape.replay(1) = 2。
