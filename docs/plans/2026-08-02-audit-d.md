# Plan —— SPEC D（并发 / 守护竞态）实施

> 输入：`docs/specs/2026-08-02-audit-d.md` rev2（round-2 pass-with-minor-caveats）。
> 顺序按 spec §9：T8 → finding 3 → finding 2 → finding 1 → 上游 SPEC 修订。

## 步骤

### 1. finding 4（ws_handler.py:113 QueueFull 加 warn + 计数）
- 改 `_on_run_changed` 的 `except asyncio.QueueFull: pass` → `logger.warning(...)` 带 run_id/action/conn.id。
- 加 `WebServer.dropped_control_frames: int` 计数器（`/api/health` 暴露面 nice-to-have；本 SPEC 仅加 counter 字段 + warning）。
- T8 grep 守门：所有「帧 payload」put_nowait 路径的 QueueFull 必 warn（`_cleanup` None-sentinel 除外）。

### 2. finding 3（_daemon_liveness.py macOS 分支）
- `/proc` 不存在（`Path("/proc").exists() is False`）→ 走 fallback：先 `os.kill(pid, 0)`，再 `subprocess.run(["ps","-p",pid,"-o","args="])` 取 cmdline 子串匹配。
- cmdline 匹配：`module_name in cmdline AND (run_id is None or ("--run-id" in cmdline and run_id in cmdline))`。
- 异常 → 保守 False（与原 OSError 行为对齐）。
- 修 docstring line 18「POSIX-only」→「POSIX（Linux /proc，macOS/BSD ps）」。
- 按裁定 R-4：spec 内 3 处「psutil」改「ps subprocess」（§3.3 I-6 / §7 / §8 T6）。

### 3. finding 2（sidechain_daemon.py 原子写 + ownership unlink）
- 加模块级 helper `_write_pidfile_atomic(pidfile, pid)` / `_unlink_pidfile_if_owner(pidfile, pid)`。
- 写：`tmp = pidfile.with_suffix(pidfile.suffix + ".tmp"); tmp.write_text(pid); os.replace(tmp, pidfile)`。`os.replace` 失败 raise（fail loud）。
- 删：读 pidfile 比对 `os.getpid()`，匹配才 unlink；不匹配 warn。
- main() 内替换两处裸 write/unlink。
- chart_daemon 不用 pidfile（grep 确认 socket-only），不抽公共模块（YAGNI）。

### 4. finding 1（auto-exit 不撕并发 run）
- `RunManager.has_nonterminal_inproc_runs() -> bool`：`any(isinstance(h, InProcessRunHandle) and h._task is not None and not h._task.done() for h in self._runs.values())`。
- `_wait_ws_autoexit(web_server, autoexit_seconds, manager)` 加第三条件 `AND not manager.has_nonterminal_inproc_runs()`；阻挡时记 debug 日志，N 由 `sum(1 for ...)` 内联计算。
- 守护异常：try/except 视为 True（保守不退）+ warning。
- 调用点 `_serve_and_run_inprocess` 把 manager 透传。
- 修 `tests/iface/cli/test_web_default_and_open.py` 5 处（D-2）：4 个 `TestWSAutoexit` + 1 个 `_raise_cancel` monkeypatch。

### 5. 上游 SPEC 修订（web-attach-and-default-spec.md）
- line 16（D4）：auto-exit 触发条件加第三 conjunct `AND 无非终态 in-process run`。
- line 79（§4 Y1 step4）：同上。

### 6. 测试
- 新建 `tests/iface/web/test_audit_d.py`：
  - T1 AC-1（设 `ORCA_WEB_AUTOEXIT_SECONDS=2`，主 run 短、并发 run 长，断言不被 cancel + debug 日志）。
  - T2 AC-1 负向（单 run 终态 + 无 WS → 退出）。
  - T3 AC-1（`--stay` + 单 run 终态 → 不退）。
  - T7 AC-4（`maxsize=1` queue 填满 → `_on_run_changed` warn）。
- 扩 `tests/iface/in_session/test_daemon_liveness.py`：T6 macOS 分支（mock `/proc` 不存在 + `ps` 返伪 cmdline → True / cmdline 不匹配 → False / pid 不存在 → False）；D-1 cmdline 含 run_id 子串的无关 arg → False。
- 扩 `tests/iface/in_session/test_sidechain_daemon.py`：T4/T5 pidfile 原子 + ownership。

### 7. 自我 review（派 code-reviewer）→ 修全 → commit + release note（M1 行为变更声明）。

## 风险 / 边界
- 依赖铁律：cli→RunManager 单向，未违。
- M1 行为变更（release note 声明）：并发 run B 卡 gate 永不终态 → 进程不自动退（需 Ctrl-C）。
- 不引 psutil；不引 pidfile flock；不改 max_concurrent / POST /api/run / lifespan。
