# Web Shell v2 —— 推倒重写（单 tape + AH 风格）

> 旧 Web 很差（agent 输出仅 60 字截断 log、无 markdown、无流式、工具不折叠、`prompt_rendered` 被忽略、多 run 外壳冗余、Replay 无用）。本次按 SDD 流程（SPEC → spec-review → clean-code → test-e2e）**推倒重写前端**，后端 tape 唯一真相源机制不动（仅 B1/B2 sanctioned 扩展）。风格对齐 AgentHarness（抄渲染层设计，严禁照搬多 store）。

## 铁律（全部 upheld）
1. 后端 tape 唯一真相源；前端 = 纯 tape 渲染（`state = fold(tape events)`，**单 Zustand store**）。
2. 接口统一（EventType codegen 一处同步）+ 过期即删（无兼容层）。
3. 设计先行，无补丁层。

## 流程
SPEC（`docs/specs/web-shell-v2-spec.md` rev2）→ spec-review-adversarial（rev1 conditional-fail，12 BLOCKER + 13 MAJOR 全闭环成 rev2，复审 conditional-pass，§5.3 表补全 39 类型 → PASS）→ clean-code-builder 分块实现（B1/B2 + Chunk A-D）→ test-coverage-e2e 真跑验证。

## 交付

### 后端 B1/B2（commit `c3a738f`）
- opencode translator **lossless**：`reasoning`→`agent_thinking`、`step_start`→新 `agent_step_started`、`agent_usage`+`reasoning_tokens`、未知 envelope→`unknown_event`（schema 37→39 EventType）。
- EventType grep 审计（所有消费者验证，新类型 default-no-op 安全）。
- B2：`supports_reasoning` capability + `ORCA_OPENCODE_REASONING_FLAGS` + executor 注入 `--thinking`/`--variant`。
- fixture 扩（含 reasoning capture）。

### 前端 Chunk A-D
- **A `84a2645`**：codegen（`scripts/gen_events_ts.py` → `events.ts` 39 自动生成 + drift guard + prebuild）、删多 run 外壳 + Replay + 旧 NodeDetail/formatLogLine、单 store（D7 seq 升序 fold + D8 no-op）、4 selectors（穷举 39）、RAF 流式（多 session）、WS resume（client+server+5 分支测试）、3 栏布局骨架。
- **B `5a26957`**：ConversationView 全渲染——markdown 栈（gfm+math+katex+prism）、per-EventType 渲染表、工具折叠/成组/展开（DiffView/FileContentView）、▎ IFF、虚拟化。
- **C `01af451`**：ChartsView（7 widget + ChartGroup + IntersectionObserver 懒挂 + 美学映射）、LogStream（react-window + 真 scrollToRow + pinned 状态机）、TopBar（live elapsed + snap 含 failed/cancelled）、AgentsRail、`useElapsedTick` singleton、Gate 模态、DAG 浮层。
- **D `7d76934`**：图片 endpoint `/api/runs/<id>/assets/<path>`（traversal+symlink guard）+ MarkdownText 图片重写、resume-fallback watchdog + `resume_ok`、bundle initial **2035KB→290KB（-86%）** lazy 拆分、StatusLine 折叠、AH 主题。
- **`60539b8`**：App.tsx 注释去 `RunsSidebar` 字面量（铁律 grep AC 闭环）。

## 验证（test-coverage-e2e 真跑裁决）
真 web server（FastAPI `orca serve`）+ 真浏览器（Playwright Chromium）+ 真 opencode+deepseek `--thinking` run + 全 39 EventType 合成 fixture。

| Must | 裁决 | 关键证据 |
|---|---|---|
| **#1 折叠/美化** | **PASS** | prompt/thinking 默认折叠、message markdown（table+LaTeX `$E=mc^2$`+code）永不折叠、工具成组 `▸ 3 tools (1 running)` + smart arg、DiffView/FileContentView、error block、status lines 全、▎ 完成态消失/运行态在 |
| **#2 图片** | **PASS** | Recharts 渲染、📊 引用行、markdown 图重写 `/api/runs/<id>/assets/` 且 200 加载、`../etc/passwd` traversal → 404 |
| **#3 LogStream 全信息** | **PASS** | 39 类型全 readable（含 `rt=` reasoning tokens 端到端证 B1/B2）、live 行数==tape、auto-scroll pinned |

TopBar / AgentsRail / Gate 模态 / DAG 浮层 / bundle 拆分 全 PASS。

**铁律 AC**：`grep replayPosition|formatLogLine|RunsSidebar|use-runs-list` == 0 ✅；单 Zustand store ✅；`events.ts`==`event.py` 39==39 ✅。

**测试**：npm **249 passed**、Python web **64 passed**。

## 已知 follow-up（非阻塞）
- `demo_task.yaml` 真 run 挂起（后端 opencode 冷启动/auth 问题，非 v2 前端缺陷；`render_chart.yaml` 同 server 7s 跑通）——单独诊断。
- DiffView 用 index-diff 非 LCS（有 rationale；AH 同款手写已生产验证）。
- ConversationView lazy chunk 1.06MB（katex/prism 可再 `manualChunks` 拆）。
- LogStream auto-scroll up-pause/jump-latest 未真跑触发（无 run 溢出视口；单测覆盖）。

## Commit
`c3a738f`（B1/B2）+ `84a2645`（A）+ `5a26957`（B）+ `01af451`（C）+ `7d76934`（D）+ `60539b8`（铁律闭环）。

SPEC：[`docs/specs/web-shell-v2-spec.md`](../specs/web-shell-v2-spec.md)。e2e 证据：`.playwright-mcp/verify-v2-*.png` + `/tmp/orca-web-v2-verify/`。
