"""data.py — dataloader leaf (kd-train-script codegen target).

Self-contained contract (CONTRACTS §1):

* Required callable: ``build_dataloader(batch_size)``.
* **Must be re-iterable**: every epoch's ``iter(dl)`` yields a fresh batch
  stream (the engine iterates it once per epoch + once for proxy_mse).
  Wrap one-shot generators in a re-iterable adapter.
* Yields ``(x, y)`` tuples whose shapes match the model contract.
* No sibling / relative imports — whitelisted stdlib + torch + numpy only.

The kd-train-script agent ports the user's data-loading logic verbatim.
"""


def build_dataloader(batch_size):
    """Re-iterable dataloader yielding ``(x, y)`` batches.  Port verbatim."""
    raise NotImplementedError(
        "kd-train-script: port the user's build_dataloader body into data.py"
    )
