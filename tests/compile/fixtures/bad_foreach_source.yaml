# ⑧ foreach.source 引用不存在的 node
name: bad_foreach_source
entry: f
nodes:
  - name: f
    kind: agent
    prompt: pf
    routes: [{to: fe}]
  - name: fe
    kind: foreach
    source: ghost.output.items
    body: {kind: agent, prompt: "eval {{ item }}"}
    routes: [{to: "$end"}]
