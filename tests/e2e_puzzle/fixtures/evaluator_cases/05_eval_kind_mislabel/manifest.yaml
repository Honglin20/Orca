project_overview:
  task_type: image classification
  purpose: toy classification baseline
model:
  location: model.py
  build_entry: build_model
  forward_signature: forward(self, x) -> hidden vector
  inputs: '[2,8,32]'
  outputs: '[2,16]'
training_and_evaluation:
  paradigm: InfoNCE metric learning
  loss: InfoNCELoss(temperature=0.07)
  metric:
    name: k-NN accuracy
    direction: higher-better
  eval_kind: classification
  evaluation_entry: train.py::eval_model
data_and_environment:
  dataset: toy
relevant_source_files: []
